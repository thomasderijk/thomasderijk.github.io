import { useMemo } from 'react';

interface SegmentedBorderProps {
  isHovered: boolean;
}

export const SegmentedBorder = ({ isHovered }: SegmentedBorderProps) => {
  // No border effect - removed
  return null;
};
